VITE_BACKEND_URL=http://localhost:3000


# Summary of What You Need to Do:
#Change your .env variable from backend_url to VITE_BACKEND_URL.
#Access it in your code using import.meta.env.VITE_BACKEND_URL.
#Ensure the .env file is at the root of your project.